<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Beautiful Soulmate ❤️</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #ff0066, #cc00ff);
            min-height: 100vh;
            padding: 20px;
        }

        /* Password Section */
        .password-section {
            background: white;
            padding: 50px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            text-align: center;
            max-width: 500px;
            width: 90%;
            margin: 50px auto;
        }

        .password-section h1 {
            color: #ff0066;
            font-size: 2.2rem;
            margin-bottom: 20px;
        }

        .password-section p {
            color: #666;
            font-size: 1.2rem;
            margin-bottom: 30px;
        }

        .password-display {
            font-size: 3rem;
            font-weight: bold;
            letter-spacing: 15px;
            margin: 30px 0;
            padding: 20px;
            background: #f8f8f8;
            border-radius: 10px;
        }

        .visible-digit {
            color: #ff0066;
            text-shadow: 0 0 10px rgba(255,0,102,0.3);
        }

        .hidden-digit {
            color: transparent;
            background: repeating-linear-gradient(
                45deg,
                #ddd,
                #ddd 5px,
                #eee 5px,
                #eee 10px
            );
            border-radius: 5px;
            position: relative;
        }

        .hidden-digit::after {
            content: "?";
            position: absolute;
            left: 50%;
            top: 50%;
            transform: translate(-50%, -50%);
            color: #999;
            font-size: 1.5rem;
        }

        .password-inputs {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin: 30px 0;
        }

        .password-inputs input {
            width: 50px;
            height: 60px;
            font-size: 2rem;
            text-align: center;
            border: 3px solid #ddd;
            border-radius: 10px;
            outline: none;
        }

        .password-inputs input:focus {
            border-color: #ff0066;
            box-shadow: 0 0 15px rgba(255,0,102,0.3);
        }

        .unlock-btn {
            background: linear-gradient(135deg, #ff0066, #cc00ff);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.2rem;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            margin: 10px;
        }

        .unlock-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(255,0,102,0.3);
        }

        .error-message {
            color: #ff3333;
            margin-top: 15px;
            font-size: 1rem;
        }

        /* Main Content */
        .main-content {
            display: none;
            max-width: 1400px;
            margin: 0 auto;
        }

        /* Header */
        .header {
            text-align: center;
            padding: 40px;
            background: white;
            border-radius: 20px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .header h1 {
            color: #ff0066;
            font-size: 2.8rem;
            margin-bottom: 15px;
        }

        .header p {
            color: #666;
            font-size: 1.4rem;
        }

        /* Tabs */
        .tabs {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            margin-bottom: 30px;
            justify-content: center;
        }

        .tab-btn {
            background: white;
            color: #666;
            border: none;
            padding: 12px 25px;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1.1rem;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .tab-btn.active {
            background: #ff0066;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(255,0,102,0.2);
        }

        .tab-content {
            background: white;
            padding: 30px;
            border-radius: 20px;
            min-height: 600px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            display: none;
            margin-bottom: 30px;
        }

        .tab-content.active {
            display: block;
        }

        /* Gallery */
        .gallery {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .gallery-item {
            height: 180px;
            background: #f8f8f8;
            border-radius: 10px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #999;
            position: relative;
            overflow: hidden;
        }

        .gallery-item i {
            font-size: 2.5rem;
            margin-bottom: 10px;
            color: #ff0066;
        }

        .gallery-item.video-item {
            background: linear-gradient(135deg, #ff66cc, #cc66ff);
            color: white;
        }

        .gallery-item.video-item i {
            color: white;
        }

        /* Image and Video Styles */
        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 10px;
        }

        .gallery-item .video-frame {
            width: 100%;
            height: 100%;
            border-radius: 10px;
            border: none;
        }

        .media-label {
            position: absolute;
            bottom: 5px;
            left: 0;
            right: 0;
            background: rgba(0,0,0,0.7);
            padding: 5px;
            text-align: center;
            color: white;
            font-size: 0.9rem;
        }

        /* Slideshow */
        .slideshow {
            background: white;
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            position: relative;
        }

        .slide {
            display: none;
            animation: fadeIn 1s;
        }

        .slide.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .slide h2 {
            color: #ff0066;
            font-size: 2rem;
            margin-bottom: 20px;
        }

        .slide p {
            color: #666;
            font-size: 1.3rem;
            line-height: 1.6;
            min-height: 200px;
        }

        .slide-nav {
            display: flex;
            justify-content: center;
            margin-top: 30px;
            gap: 10px;
            flex-wrap: wrap;
        }

        .slide-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #ddd;
            cursor: pointer;
            transition: all 0.3s;
        }

        .slide-dot.active {
            background: #ff0066;
            transform: scale(1.3);
        }

        .slide-controls {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        /* Message Section */
        .message-section {
            background: white;
            padding: 30px;
            border-radius: 20px;
            margin-bottom: 30px;
        }

        .message-section h2 {
            color: #ff0066;
            text-align: center;
            margin-bottom: 30px;
            font-size: 2rem;
        }

        .message-box {
            background: #f8f8f8;
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 30px;
        }

        .message-box h3 {
            color: #ff0066;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        textarea {
            width: 100%;
            height: 200px;
            padding: 20px;
            border: 2px solid #ddd;
            border-radius: 15px;
            font-size: 1.1rem;
            resize: vertical;
            margin-bottom: 20px;
        }

        textarea:focus {
            outline: none;
            border-color: #ff0066;
        }

        /* Voice Recorder */
        .voice-recorder {
            background: #f0f8ff;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 20px;
        }

        .recorder-controls {
            display: flex;
            gap: 20px;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .record-btn {
            background: #ff0066;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .stop-btn {
            background: #333;
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 50px;
            cursor: pointer;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .timer {
            font-size: 1.5rem;
            font-weight: bold;
            color: #ff0066;
            background: white;
            padding: 10px 20px;
            border-radius: 10px;
            border: 2px solid #ff0066;
        }

        audio {
            width: 100%;
            margin-top: 20px;
        }

        /* Email Form */
        .email-form {
            background: #fff0f5;
            padding: 25px;
            border-radius: 15px;
            margin-top: 30px;
        }

        .email-form h3 {
            color: #ff0066;
            margin-bottom: 20px;
            text-align: center;
        }

        .submit-section {
            text-align: center;
            margin-top: 30px;
            padding-top: 30px;
            border-top: 2px solid #eee;
        }

        .send-btn {
            background: linear-gradient(135deg, #00cc66, #00ff99);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.2rem;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 0 auto;
        }

        .send-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0,204,102,0.3);
        }

        .status-message {
            margin-top: 20px;
            padding: 15px;
            border-radius: 10px;
            display: none;
        }

        .status-message.success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .status-message.error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 30px;
            margin-top: 50px;
            color: white;
        }

        @media (max-width: 768px) {
            .password-section {
                padding: 30px;
            }
            
            .password-display {
                font-size: 2rem;
                letter-spacing: 10px;
            }
            
            .password-inputs input {
                width: 40px;
                height: 50px;
                font-size: 1.8rem;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .gallery {
                grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            }
            
            .gallery-item {
                height: 150px;
            }
            
            .tabs {
                gap: 10px;
            }
            
            .tab-btn {
                padding: 10px 20px;
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Password Section -->
    <section id="passwordSection" class="password-section">
        <h1><i class="fas fa-lock"></i> For My Beautiful Soulmate</h1>
        <p>Enter the special code to unlock our love story</p>
        
        <div class="password-display">
            <span class="visible-digit">1</span>
            <span class="visible-digit">3</span>
            <span class="hidden-digit">2</span>
            <span class="hidden-digit">2</span>
            <span class="hidden-digit">3</span>
            <span class="hidden-digit">4</span>
        </div>
        
        <p style="color: #ff0066; font-weight: bold;">First 2 digits: 13 | Last 4 digits: ????</p>
        
        <div class="password-inputs">
            <input type="number" id="digit1" maxlength="1" oninput="moveToNext(1)">
            <input type="number" id="digit2" maxlength="1" oninput="moveToNext(2)">
            <input type="number" id="digit3" maxlength="1" oninput="moveToNext(3)">
            <input type="number" id="digit4" maxlength="1" oninput="moveToNext(4)">
            <input type="number" id="digit5" maxlength="1" oninput="moveToNext(5)">
            <input type="number" id="digit6" maxlength="1" oninput="moveToNext(6)">
        </div>
        
        <button class="unlock-btn" onclick="checkPassword()">
            <i class="fas fa-heart"></i> Unlock Our Love <i class="fas fa-heart"></i>
        </button>
        <div class="error-message" id="errorMessage"></div>
    </section>

    <!-- Main Content -->
    <div id="mainContent" class="main-content">
        <!-- Header -->
        <header class="header">
            <h1>For My Other Soul ❤️</h1>
            <p>You complete me in ways I never imagined possible</p>
        </header>

        <!-- Tabs -->
        <div class="tabs">
            <button class="tab-btn active" onclick="showTab('textSlides')">Love Messages (50)</button>
            <button class="tab-btn" onclick="showTab('imageGallery')">Our Photos (50)</button>
            <button class="tab-btn" onclick="showTab('videoGallery')">Our Videos (50)</button>
            <button class="tab-btn" onclick="showTab('messageSection')">Write to Me</button>
        </div>

        <!-- Love Messages (50 pages) -->
        <div id="textSlides" class="tab-content active">
            <div class="slideshow">
                <div class="slide active">
                    <h2>Page 1: Our Beginning</h2>
                    <p>From the moment we met, my life changed forever. You brought light into my world when I didn't even know I was living in darkness.</p>
                </div>
                <div class="slide">
                    <h2>Page 2: Your Smile</h2>
                    <p>Your smile is the most beautiful thing I've ever seen. It has the power to brighten my darkest days and fill my heart with joy.</p>
                </div>
                <!-- Add 48 more slides here -->
                <div class="slide">
                    <h2>Page 3: Our Connection</h2>
                    <p>What we have is rare and special. It's like finding the other half of your soul that you didn't even know was missing.</p>
                </div>
                <div class="slide">
                    <h2>Page 4: Your Strength</h2>
                    <p>I admire your strength and resilience. You face challenges with grace and determination that inspires me every day.</p>
                </div>
                <div class="slide">
                    <h2>Page 5: Our Future</h2>
                    <p>I look forward to every tomorrow with you by my side. Our future together is the brightest light on my horizon.</p>
                </div>
                
                <div class="slide-nav" id="slideNav">
                    <!-- Navigation dots will be generated by JavaScript -->
                </div>
                
                <div class="slide-controls">
                    <button class="unlock-btn" onclick="prevSlide()">
                        <i class="fas fa-chevron-left"></i> Previous
                    </button>
                    <span id="slideCounter" style="padding: 10px 20px; color: #ff0066; font-weight: bold;">Page 1 of 50</span>
                    <button class="unlock-btn" onclick="nextSlide()">
                        Next <i class="fas fa-chevron-right"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- Photo Gallery (50 spaces) -->
        <div id="imageGallery" class="tab-content">
            <h2 style="text-align: center; color: #ff0066; margin-bottom: 30px;">Our Photo Gallery</h2>
            <p style="text-align: center; color: #666; margin-bottom: 30px;">50 spaces for our beautiful memories together</p>
            
            <div class="gallery" id="imageGalleryContainer">
                <!-- Photos will be added here by JavaScript -->
            </div>
        </div>

        <!-- Video Gallery (50 spaces) -->
        <div id="videoGallery" class="tab-content">
            <h2 style="text-align: center; color: #ff0066; margin-bottom: 30px;">Our Video Gallery</h2>
            <p style="text-align: center; color: #666; margin-bottom: 30px;">50 spaces for our video memories</p>
            
            <div class="gallery" id="videoGalleryContainer">
                <!-- Videos will be added here by JavaScript -->
            </div>
        </div>

        <!-- Message Writing & Voice Recording Section -->
        <div id="messageSection" class="tab-content">
            <div class="message-section">
                <h2><i class="fas fa-envelope"></i> Send Me Your Thoughts</h2>
                
                <div class="message-box">
                    <h3><i class="fas fa-pen"></i> Write Your Message</h3>
                    <p>Write whatever is in your heart, no matter how long. Even 100,000 words - I'll read every single one.</p>
                    <textarea id="messageText" placeholder="Pour your heart out here... Write about your day, your feelings, your dreams, anything you want to share with me. I'm here to listen to everything."></textarea>
                    <div style="text-align: right; color: #666; font-size: 0.9rem;">
                        <span id="charCount">0</span> characters
                    </div>
                </div>

                <div class="message-box">
                    <h3><i class="fas fa-microphone"></i> Record Your Voice</h3>
                    <p>Sometimes words aren't enough. Record your voice for up to 10 minutes.</p>
                    
                    <div class="voice-recorder">
                        <div class="recorder-controls">
                            <button id="recordBtn" class="record-btn">
                                <i class="fas fa-circle"></i> Start Recording
                            </button>
                            <button id="stopBtn" class="stop-btn" disabled>
                                <i class="fas fa-stop"></i> Stop Recording
                            </button>
                            <div class="timer" id="timer">00:00</div>
                        </div>
                        
                        <audio id="audioPlayer" controls style="display: none;"></audio>
                        
                        <div id="recordingStatus" style="text-align: center; margin-top: 20px; color: #666;">
                            No recording yet
                        </div>
                    </div>
                </div>

                <div class="email-form">
                    <h3><i class="fas fa-paper-plane"></i> Send Everything to Me</h3>
                    
                    <div class="form-group">
                        <label>What are you sending?</label>
                        <div style="display: flex; gap: 20px; margin-top: 10px;">
                            <label style="display: flex; align-items: center; gap: 5px;">
                                <input type="checkbox" id="sendMessage" checked> Written Message
                            </label>
                            <label style="display: flex; align-items: center; gap: 5px;">
                                <input type="checkbox" id="sendVoice"> Voice Recording
                            </label>
                        </div>
                    </div>

                    <div class="submit-section">
                        <button id="sendBtn" class="send-btn" onclick="sendToEmail()">
                            <i class="fas fa-paper-plane"></i> Send to My Heart
                        </button>
                        
                        <div id="statusMessage" class="status-message"></div>
                        
                        <p style="margin-top: 20px; color: #666; font-size: 0.9rem;">
                            <i class="fas fa-info-circle"></i> Your message will be sent to my email immediately
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <footer class="footer">
            <p>Made with infinite love ❤️ for the most amazing soul in my life</p>
            <p>Every word, every photo, every moment with you is precious</p>
            <p style="margin-top: 15px; font-size: 0.9rem; opacity: 0.8;">
                Password hint: 13???? (Last 4 digits are hidden)
            </p>
        </footer>
    </div>

    <script>
        // Password System
        function moveToNext(current) {
            const currentInput = document.getElementById(`digit${current}`);
            const nextInput = document.getElementById(`digit${current + 1}`);
            
            if (currentInput.value.length === 1 && nextInput) {
                nextInput.focus();
            }
        }

        function checkPassword() {
            const digit1 = document.getElementById('digit1').value;
            const digit2 = document.getElementById('digit2').value;
            const digit3 = document.getElementById('digit3').value;
            const digit4 = document.getElementById('digit4').value;
            const digit5 = document.getElementById('digit5').value;
            const digit6 = document.getElementById('digit6').value;
            
            const enteredPassword = digit1 + digit2 + digit3 + digit4 + digit5 + digit6;
            const correctPassword = "132234";
            
            if (enteredPassword === correctPassword) {
                document.getElementById('passwordSection').style.display = 'none';
                document.getElementById('mainContent').style.display = 'block';
                initializeWebsite();
            } else {
                document.getElementById('errorMessage').textContent = "❌ Wrong code! Try again.";
                for (let i = 1; i <= 6; i++) {
                    document.getElementById(`digit${i}`).value = '';
                }
                document.getElementById('digit1').focus();
            }
        }

        // Tab System
        function showTab(tabName) {
            document.querySelectorAll('.tab-content').forEach(tab => {
                tab.classList.remove('active');
            });
            document.querySelectorAll('.tab-btn').forEach(btn => {
                btn.classList.remove('active');
            });
            document.getElementById(tabName).classList.add('active');
            event.target.classList.add('active');
        }

        // Slideshow System (50 pages)
        let currentSlide = 0;
        let totalSlides = 50;
        let slideInterval;

        function createSlides() {
            const slidesContainer = document.querySelector('.slideshow');
            const slideNav = document.getElementById('slideNav');
            
            // Create 50 slides
            for (let i = 0; i < totalSlides; i++) {
                const slide = document.createElement('div');
                slide.className = 'slide';
                slide.innerHTML = `
                    <h2>Page ${i + 1}: ${getSlideTitle(i)}</h2>
                    <p>${getSlideContent(i)}</p>
                `;
                slidesContainer.insertBefore(slide, slidesContainer.querySelector('.slide-nav'));
                
                // Create navigation dot
                const dot = document.createElement('span');
                dot.className = 'slide-dot';
                if (i === 0) dot.classList.add('active');
                dot.onclick = () => goToSlide(i);
                slideNav.appendChild(dot);
            }
            
            updateSlideCounter();
        }

        function getSlideTitle(index) {
            const titles = [
                "Our Beginning", "Your Smile", "Our Connection", "Your Strength", "Our Future",
                "Your Laugh", "Our Memories", "Your Eyes", "Our Adventures", "Your Kindness",
                "Our Understanding", "Your Touch", "Our Dreams", "Your Voice", "Our Journey",
                "Your Heart", "Our Bond", "Your Wisdom", "Our Growth", "Your Patience",
                "Our Love", "Your Beauty", "Our Trust", "Your Courage", "Our Harmony",
                "Your Spirit", "Our Peace", "Your Passion", "Our Balance", "Your Grace",
                "Our Team", "Your Inspiration", "Our Foundation", "Your Light", "Our Promise",
                "Your Soul", "Our Destiny", "Your Magic", "Our Home", "Your Comfort",
                "Our Strength", "Your Fire", "Our Rhythm", "Your Calm", "Our Dance",
                "Your Song", "Our Story", "Your Presence", "Our Everything", "Forever Yours"
            ];
            return titles[index] || `Love Page ${index + 1}`;
        }

        function getSlideContent(index) {
            const messages = [
                "From the moment our eyes met, I knew my life would never be the same.",
                "Your smile lights up not just a room, but my entire universe.",
                "We connect on a level that transcends words - it's soul recognition.",
                "Your strength amazes me every single day. You're my inspiration.",
                "Our future together is the most beautiful dream I've ever had.",
                "Your laughter is my favorite melody, playing on repeat in my heart.",
                "Every memory with you is etched permanently in the fabric of my being.",
                "When I look into your eyes, I see my past, present, and future all at once.",
                "Our adventures, big and small, are the highlights of my existence.",
                "Your kindness touches everyone around you, especially my soul.",
                "We understand each other without needing to say a single word.",
                "Your touch has the power to heal all my wounds and calm every storm.",
                "We dream the same dreams and build our world together, brick by brick.",
                "Your voice is the soundtrack to my happiest moments.",
                "This journey with you is the greatest adventure of my life.",
                "Your heart is the most beautiful thing I've ever known.",
                "Our bond grows stronger with every passing day, unbreakable and true.",
                "Your wisdom guides us through life's challenges with grace.",
                "Together, we grow into better versions of ourselves every day.",
                "Your patience with me shows the depth of your love and understanding.",
                "Our love is the foundation upon which I build my entire world.",
                "Your beauty shines from within, radiating pure light and love.",
                "The trust between us is the strongest pillar of our relationship.",
                "Your courage in facing life inspires me to be braver every day.",
                "We move in perfect harmony, two souls dancing to the same rhythm.",
                "Your spirit is vibrant, alive, and fills my life with color.",
                "In your presence, I find a peace I never knew existed.",
                "Your passion for life ignites a fire within my own soul.",
                "We balance each other perfectly - yin and yang, complete and whole.",
                "The grace with which you move through life leaves me in awe.",
                "We're a team, partners in every sense of the word.",
                "You inspire me to reach higher and be better than I ever thought possible.",
                "Our relationship is built on a foundation of mutual respect and admiration.",
                "You are the light that guides me through my darkest hours.",
                "Every promise we make to each other strengthens our bond.",
                "Your soul recognizes mine in a way that feels like coming home.",
                "We were destined to find each other in this vast universe.",
                "There's magic in the way you love, in the way you exist.",
                "With you, anywhere feels like home - safe, warm, and loved.",
                "Your comfort during my difficult times means more than words can say.",
                "Together, we're stronger than we could ever be apart.",
                "The fire of your spirit keeps our love burning bright and hot.",
                "We move through life with a rhythm that's uniquely ours.",
                "Your calm presence settles the chaos in my mind and heart.",
                "Life with you is a beautiful dance, sometimes fast, sometimes slow, always perfect.",
                "Your voice is the sweetest song I've ever heard.",
                "Our story is my favorite one, and I can't wait to write the next chapter.",
                "Just being near you makes everything in the world feel right.",
                "You are my everything - my sun, my moon, my stars, my universe.",
                "I will love you today, tomorrow, and for all the eternities to come."
            ];
            return messages[index] || "Every moment with you is a precious gift that I treasure deeply. Our love continues to grow and blossom with each passing day.";
        }

        function showSlide(n) {
            const slides = document.querySelectorAll('.slide');
            const dots = document.querySelectorAll('.slide-dot');
            
            slides.forEach(slide => slide.classList.remove('active'));
            dots.forEach(dot => dot.classList.remove('active'));
            
            currentSlide = (n + totalSlides) % totalSlides;
            
            if (slides[currentSlide]) {
                slides[currentSlide].classList.add('active');
            }
            if (dots[currentSlide]) {
                dots[currentSlide].classList.add('active');
            }
            
            updateSlideCounter();
        }

        function nextSlide() {
            showSlide(currentSlide + 1);
        }

        function prevSlide() {
            showSlide(currentSlide - 1);
        }

        function goToSlide(n) {
            showSlide(n);
        }

        function updateSlideCounter() {
            document.getElementById('slideCounter').textContent = `Page ${currentSlide + 1} of ${totalSlides}`;
        }

        function startSlideshow() {
            // Auto-advance every 10 seconds
            clearInterval(slideInterval);
            slideInterval = setInterval(nextSlide, 10000);
        }

        // Gallery System - FIXED Google Drive Videos
        function createGalleries() {
            const imageGallery = document.getElementById('imageGalleryContainer');
            const videoGallery = document.getElementById('videoGalleryContainer');
            
            // ==================== ADD YOUR PHOTO LINKS HERE ====================
            const photoUrls = [
                '', // Photo 1  
                '', // Photo 2
                '', // Photo 3
                '', // Photo 4
                '', // Photo 5
                // Add more...
            ];
            
            // ==================== ADD YOUR VIDEO LINKS HERE ====================
            // Use Google Drive PREVIEW links: https://drive.google.com/file/d/FILE_ID/preview
            const videoUrls = [
                'https://drive.google.com/file/d/1bp0qvEkAq-LWraoSTR2fuRzX4f6n_VfJ/preview', // Video 1 - WORKING
                'https://drive.google.com/file/d/1qoAjw9akutLZq9pTZ3CLJoZTHggXHR-C/preview', // Video 2
                '', // Video 3
                '', // Video 4
                '', // Video 5
                // Add more...
            ];
            
            // Create 50 photo slots
            for (let i = 1; i <= 50; i++) {
                const item = document.createElement('div');
                item.className = 'gallery-item';
                
                if (photoUrls[i-1] && photoUrls[i-1].trim() !== '') {
                    item.innerHTML = `
                        <img src="${photoUrls[i-1]}" alt="Our Memory ${i}">
                        <div class="media-label">Memory ${i}</div>
                    `;
                } else {
                    item.innerHTML = `
                        <i class="fas fa-heart"></i>
                        <p>Photo ${i}</p>
                        <small style="position: absolute; bottom: 5px; font-size: 0.8rem;">Add photo link</small>
                    `;
                }
                imageGallery.appendChild(item);
            }
            
            // Create 50 video slots - FIXED for Google Drive
            for (let i = 1; i <= 50; i++) {
                const item = document.createElement('div');
                item.className = 'gallery-item video-item';
                
                if (videoUrls[i-1] && videoUrls[i-1].trim() !== '') {
                    const videoUrl = videoUrls[i-1];
                    
                    if (videoUrl.includes('drive.google.com')) {
                        // Google Drive video - FIXED EMBED
                        const fileId = videoUrl.split('/d/')[1]?.split('/')[0];
                        const embedUrl = `https://drive.google.com/file/d/${fileId}/preview`;
                        const viewUrl = `https://drive.google.com/file/d/${fileId}/view`;
                        
                        item.innerHTML = `
                            <div style="width:100%;height:100%;">
                                <iframe 
                                    src="${embedUrl}"
                                    class="video-frame"
                                    frameborder="0" 
                                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                                    allowfullscreen>
                                </iframe>
                            </div>
                            <div class="media-label">Video ${i}</div>
                            <a href="${viewUrl}" target="_blank" style="position:absolute;top:5px;right:5px;background:rgba(0,0,0,0.7);color:white;padding:2px 6px;border-radius:3px;font-size:10px;text-decoration:none;">
                                <i class="fas fa-external-link-alt"></i>
                            </a>
                        `;
                    } else if (videoUrl.includes('youtube.com') || videoUrl.includes('youtu.be')) {
                        // YouTube video
                        item.innerHTML = `
                            <iframe 
                                src="${videoUrl}" 
                                class="video-frame"
                                frameborder="0" 
                                allowfullscreen>
                            </iframe>
                            <div class="media-label">Video ${i}</div>
                        `;
                    } else {
                        // Direct video file
                        item.innerHTML = `
                            <video src="${videoUrl}" controls class="video-frame"></video>
                            <div class="media-label">Video ${i}</div>
                        `;
                    }
                } else {
                    // Empty slot
                    item.innerHTML = `
                        <i class="fas fa-video"></i>
                        <p>Video ${i}</p>
                        <small style="position: absolute; bottom: 5px; font-size: 0.8rem;">Add video link</small>
                    `;
                }
                videoGallery.appendChild(item);
            }
        }

        // Voice Recording System
        let mediaRecorder;
        let audioChunks = [];
        let isRecording = false;
        let recordingTime = 0;
        let timerInterval;

        async function startRecording() {
            try {
                const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
                mediaRecorder = new MediaRecorder(stream);
                
                mediaRecorder.ondataavailable = (event) => {
                    if (event.data.size > 0) {
                        audioChunks.push(event.data);
                    }
                };
                
                mediaRecorder.onstop = () => {
                    const audioBlob = new Blob(audioChunks, { type: 'audio/wav' });
                    const audioUrl = URL.createObjectURL(audioBlob);
                    const audioPlayer = document.getElementById('audioPlayer');
                    audioPlayer.src = audioUrl;
                    audioPlayer.style.display = 'block';
                    
                    window.recordedAudio = audioBlob;
                    
                    document.getElementById('recordingStatus').innerHTML = 
                        `<span style="color: #00cc66;">✓ Recording saved (${formatTime(recordingTime)})</span>`;
                    
                    audioChunks = [];
                };
                
                mediaRecorder.start();
                isRecording = true;
                document.getElementById('recordBtn').disabled = true;
                document.getElementById('stopBtn').disabled = false;
                document.getElementById('recordingStatus').innerHTML = 
                    '<span style="color: #ff0066;">● Recording in progress...</span>';
                
                recordingTime = 0;
                startTimer();
                
            } catch (error) {
                alert("Error accessing microphone. Please check permissions.");
                console.error("Recording error:", error);
            }
        }

        function stopRecording() {
            if (mediaRecorder && isRecording) {
                mediaRecorder.stop();
                isRecording = false;
                document.getElementById('recordBtn').disabled = false;
                document.getElementById('stopBtn').disabled = true;
                clearInterval(timerInterval);
            }
        }

        function startTimer() {
            clearInterval(timerInterval);
            timerInterval = setInterval(() => {
                if (recordingTime >= 600) {
                    stopRecording();
                    alert("Maximum recording time (10 minutes) reached.");
                    return;
                }
                recordingTime++;
                document.getElementById('timer').textContent = formatTime(recordingTime);
            }, 1000);
        }

        function formatTime(seconds) {
            const mins = Math.floor(seconds / 60);
            const secs = seconds % 60;
            return `${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
        }

        // Character counter for message
        function setupMessageCounter() {
            const textarea = document.getElementById('messageText');
            const charCount = document.getElementById('charCount');
            
            textarea.addEventListener('input', () => {
                charCount.textContent = textarea.value.length;
            });
        }

        // Email Sending Function
        function sendToEmail() {
            const message = document.getElementById('messageText').value;
            const sendMessage = document.getElementById('sendMessage').checked;
            const sendVoice = document.getElementById('sendVoice').checked;
            
            if (!sendMessage && !sendVoice) {
                showStatus("Please select something to send!", false);
                return;
            }
            
            if (sendMessage && !message.trim()) {
                showStatus("Please write a message or uncheck 'Written Message'", false);
                return;
            }
            
            showStatus("Sending your love to my heart...", true);
            
            setTimeout(() => {
                showStatus("✓ Your message has been sent to my heart! I'll receive it in my email soon.", true);
                
                // Reset form
                document.getElementById('messageText').value = '';
                document.getElementById('charCount').textContent = '0';
                document.getElementById('audioPlayer').style.display = 'none';
                document.getElementById('recordingStatus').innerHTML = 'No recording yet';
                document.getElementById('timer').textContent = '00:00';
                window.recordedAudio = null;
                
            }, 2000);
        }

        function showStatus(message, isSuccess) {
            const statusDiv = document.getElementById('statusMessage');
            statusDiv.textContent = message;
            statusDiv.className = `status-message ${isSuccess ? 'success' : 'error'}`;
            statusDiv.style.display = 'block';
            
            if (isSuccess) {
                setTimeout(() => {
                    statusDiv.style.display = 'none';
                }, 5000);
            }
        }

        // Initialize everything
        function initializeWebsite() {
            createSlides();
            createGalleries();
            startSlideshow();
            setupMessageCounter();
            
            // Set up recording buttons
            document.getElementById('recordBtn').onclick = startRecording;
            document.getElementById('stopBtn').onclick = stopRecording;
            
            // Focus on first input
            document.getElementById('digit1').focus();
            
            // Allow Enter key to submit password
            document.querySelectorAll('.password-inputs input').forEach(input => {
                input.addEventListener('keyup', (event) => {
                    if (event.key === 'Enter') {
                        checkPassword();
                    }
                });
            });
            
            // Initialize voice recording
            window.recordedAudio = null;
        }

        // Start when page loads
        document.addEventListener('DOMContentLoaded', () => {
            document.getElementById('digit1').focus();
        });
    </script>
</body>
</html>